// A test for a viewer of a chat

// Path: clarify/__tests__/viewer.test.js

// Viewer is a user who is not participating in the chat but is observing it

// Create a user with viewer role
//const { createUser } = require('../src/services/userService');

// describe('Viewer', () => {
//     let viewer;
//     let chat;

//     beforeEach(async () => {
//         const email = `